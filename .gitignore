# build / packaging artifacts
dist/
build/
*.egg-info/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# benchmark scratch (generated data + local results; CI uploads results as an artifact)
bench/_bench_data.csv
bench/_out_*.csv
bench/RESULTS.md

# local envs
.venv/
venv/

# example outputs
examples/out/
examples/*_out.csv

# local notes (not for the public repo)
PROJECT_NOTES.md
BACKLOG.md
feedback.txt

# secrets / credentials - NEVER commit or ship
*accessKeys*.csv
*credential*
.pypirc
*.pem
.env
*token*.txt

# local screenshots / scratch images - NEVER commit (may show local paths / data)
Screenshot*.png
*.png
*.jpg
*.jpeg
*.gif
*.webp

# marketing drafts (not for the public repo)
LAUNCH_POSTS.md

# planning docs (private for now; can be published later)
ROADMAP.md
Feedback2.txt
